spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/fdmatch_core_test
    username: testuser
    password: testpassword

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false
