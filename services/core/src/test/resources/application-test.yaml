spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fdmatch_core_test
    username: testuser
    password: testpassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

clients:
  users-service:
    url: http://localhost:8080

logging:
  level:
    root: DEBUG