meta {
  name: match/next
  type: http
  seq: 2
}

post {
  url: {{uat_host}}/core/match/next
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  {
    "batch_size": 10
  }
}
